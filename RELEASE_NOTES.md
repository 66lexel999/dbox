D BOX 1.0.1

• Fixed: the window title bar now shows the D BOX icon (was a generic icon).
• Browser extension: now detects videos inside embedded players (uqload and
  similar) and captures the real stream — like IDM. Reload the extension in
  Brave to get this.
• Faster, more reliable video capture across sites.
